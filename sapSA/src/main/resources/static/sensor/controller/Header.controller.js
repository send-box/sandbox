sap.ui.define([
		"tips/common/controller/Header.controller"
	], function (HeaderController) {
		"use strict";
	
	return HeaderController.extend("tips.sensor.controller.Header", {
		
		// 초기 함수(공통 헤더에서 호출)
		onInitHeader : function() {
			
		}
	});

}, /* bExport= */ true);